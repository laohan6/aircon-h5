# 饭饭小窝的空调

一个纯静态前端 Demo，包含空调机身、开关控制、温度调节、模式切换和对应动画效果。

## 本地预览

直接用浏览器打开 `index.html` 即可。

## GitHub Pages

推送到 GitHub 仓库后，在仓库的 `Settings -> Pages` 中选择 `GitHub Actions` 作为发布来源。之后每次推送到 `main` 分支都会自动部署。
